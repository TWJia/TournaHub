1.	Open the backend folder in command panel and type npm init -y
2.	Afterwards type npm i mongodb express cors dotenv in command panel
3.	Now, Open the frontend folder in command panel and type npm install
4. 	Afterwards type npm run dev in command panel
5. 	Open web browser with link http://localhost:5173/ , if not, you can open it manually.
